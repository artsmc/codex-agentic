# Python dependencies for document-hub skill scripts
# These scripts use only Python standard library to minimize dependencies

# No external dependencies required!
# All scripts use only built-in Python modules:
# - json (JSON parsing)
# - sys (command-line args)
# - re (regular expressions)
# - pathlib (file path handling)
# - subprocess (git commands)
# - collections (Counter, etc.)

# Optional: For enhanced Mermaid validation (not required for basic functionality)
# mermaid-py==0.1.0

# To install (if needed):
# pip install -r requirements.txt
